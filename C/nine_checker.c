#include <stdio.h>

int main(void)
{
    int num, i, r;
    do
    {
        printf("Input Number Here: ");
        scanf("%d", &num);
    } 
    while (num < 0);
    
    if ()
    {

    }

    else if ()
}